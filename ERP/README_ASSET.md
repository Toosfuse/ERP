# 📦 سیستم مدیریت اموال

## 🎯 معماری

### Models
- **AssestUser**: کاربران/واحدهای مالک اموال
- **AssestCategory**: دستهبندی اموال (درختی)
- **Asset**: اموال اصلی
- **AssetHistory**: تاریخچه تحویل اموال

### Views
- **RegisterUser.cshtml**: ثبت و مدیریت کاربران/واحدها
- **RegisterCategory.cshtml**: مدیریت دستهبندی اموال
- **RegisterAsset.cshtml**: ثبت و مدیریت اموال
- **AssetHistory.cshtml**: تاریخچه تحویل اموال

---

## 🚀 مراحل اجرا

### 1. اعمال Migration
```bash
Add-Migration AssetManagementRefactor
Update-Database
```

### 2. تست کردن
```
/Assest/RegisterUser - ثبت کاربران
/Assest/RegisterCategory - ثبت دستهبندی
/Assest/RegisterAsset - ثبت اموال
/Assest/AssetHistory - تاریخچه
```

---

## ✨ ویژگیهای اصلی

✅ ثبت کاربران/واحدها
✅ مدیریت دستهبندی اموال (درختی)
✅ ثبت اموال با مالک فعلی
✅ تاریخچه تحویل اموال
✅ نمایش مالک قبلی و فعلی
✅ Kendo Grid برای نمایش
✅ جستجو و فیلتر
✅ Pagination

---

## 📊 جریان کار

```
1. ثبت کاربر/واحد
   ↓
2. تعریف دستهبندی اموال
   ↓
3. ثبت اموال (مالک اولیه)
   ↓
4. ثبت تحویل اموال
   ↓
5. مالک فعلی اپدیت شود
```

---

## 🔗 API Endpoints

### Asset
- `GET /Assest/RegisterAsset` - صفحه ثبت
- `GET /Assest/Assets_Read` - لیست اموال
- `POST /Assest/Assets_Create` - ثبت اموال
- `POST /Assest/Assets_Update` - ویرایش اموال
- `POST /Assest/Assets_Destroy` - حذف اموال
- `GET /Assest/GetCategories` - لیست دستهبندیها
- `GET /Assest/GetUsers` - لیست کاربران

### AssetHistory
- `GET /Assest/AssetHistory` - صفحه تاریخچه
- `GET /Assest/AssetHistory_Read` - لیست تحویلها
- `POST /Assest/AssetHistory_Create` - ثبت تحویل
- `GET /Assest/GetAssets` - لیست اموال

---

## 📝 نکات مهم

1. **کد اموال یکتا است** - نمیتوان دو اموال با کد یکسان ثبت کرد
2. **مالک فعلی اپدیت میشود** - هنگام ثبت تحویل، مالک فعلی خودکار تغییر میکند
3. **تاریخچه حفظ میشود** - تمام تحویلها برای ردیابی ثبت میشوند
4. **حذف Cascade** - حذف اموال تاریخچه آن را نیز حذف میکند

---

**نسخه:** 1.0
**وضعیت:** ✅ آماده برای استفاده
